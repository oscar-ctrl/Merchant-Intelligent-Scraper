name: Get Information

on:
  schedule:
    # Test: 17:40 y 17:45 hora España
    # GitHub Actions usa UTC
    # España ahora está en CEST = UTC+2
    - cron: "40 15 * * *"
    - cron: "45 15 * * *"

  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: get-information
  cancel-in-progress: false

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run scraper
        run: |
          python scripts/g_script.py

      - name: Upload CSV artifact
        uses: actions/upload-artifact@v4
        with:
          name: glovo-store-data
          path: outputs/*.csv
          if-no-files-found: warn

      - name: Commit CSV to repository
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add outputs/*.csv

          if git diff --cached --quiet; then
            echo "No CSV changes to commit."
          else
            git commit -m "Add daily store data CSV"
            git push
          fi